# Required GitHub Secrets

- `SSH_HOST`
- `SSH_PORT`
- `SSH_USER`
- `SSH_KEY`
- `DEPLOY_PATH`

`DEPLOY_PATH` should point to the directory containing the uploaded `bite-api/` and `infra/` folders on the home server.
